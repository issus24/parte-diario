[phases.setup]
nixPkgs = ["python311Full", "python311Packages.pip", "python311Packages.setuptools"]

[phases.install]
cmds = ["python3 -m pip install -r backend/requirements.txt"]

[start]
cmd = "python3 -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-3001} --app-dir backend"
